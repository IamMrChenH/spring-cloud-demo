# 应用名称
spring:
  application:
    name: spring-cloud-config-client
  cloud:
    config:
      uri: http://localhost:8100/
      profile: dev
      label: master
      name: spring-cloud-config-client
